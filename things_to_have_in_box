git (install)
tig (git branching)
rbenv (install, rehash, ruby-build plugin) set global version of ruby to 2.1.5
install bundler with ruby 2.1.5
vim (install)
pathogen ( vim plugin management tools ) 
  |
  --- 1. NERDtree
  --- 2. ctrlp
  ----3. Rails plugin

postgres (install)
  |
  ------- sudo apt-get install postgresql postgresql-server-dev-9.1
  ------- sudo apt-get install postgresql postgresql-contrib
  ------- one DB role with createDB permissions ( create role; alter role <name> CREATEDB; alter role <name> PASSWORD '<password>';

copy all the dot files

javascript (node)

aliases ( bash, git etc.)

rails 4.2.0 does not bind 0.0.0.0:3000 but 127.0.0.1:3000
create new ./config/database.yml file for every project

rails 4.2.0 support for rspec-rails start from 3.1.0
